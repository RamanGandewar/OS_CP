# Backend - Phase 1 Basic CRM

## Setup

```bash
cd Backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python database\seed_data.py
python app.py
```

Backend runs on `http://127.0.0.1:5000`.
