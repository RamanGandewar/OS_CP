# Lock Contention Analysis

Use the synchronization monitor data to analyze:

- which resources are most frequently contended
- average wait queue size
- which synchronization primitive resolves each conflict
